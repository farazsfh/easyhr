package ProgramFiles.GuiFiles;

public interface PanelSwitchListener {

    /**
     * Bring a specific panel into view when called
     *
     * @param panel The panel to emit
     */
    void panelEmitted(String panel);
}
